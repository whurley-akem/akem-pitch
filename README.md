# akem-pitch

Investor pitch deck for Akem, built as a Reveal.js slide site.

## Run locally

Open `index.html` directly, or run a simple static server.

## Deploy

This repo is configured for Vercel static deployment.
